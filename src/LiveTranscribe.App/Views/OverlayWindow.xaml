<Window x:Class="LiveTranscribe.App.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Live Transcribe"
        Width="320" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True">
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="14"
            MouseLeftButtonDown="OnDragHandle">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.5" />
        </Border.Effect>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Width="44" Height="44" Command="{Binding ToggleRecordingCommand}"
                        Cursor="Hand" BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                    <Setter Property="Background" Value="{StaticResource RecordingBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="22">
                                <Ellipse Width="16" Height="16" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Live Transcribe" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                    <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextBrush}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <Button Grid.Column="2" Content="⚙" FontSize="16" Width="32" Height="32"
                        Click="OnSettingsClick" Background="Transparent" Foreground="{StaticResource MutedBrush}"
                        BorderThickness="0" Cursor="Hand" ToolTip="Einstellungen" />
            </Grid>

            <ProgressBar Height="4" Margin="0,10,0,0" Minimum="0" Maximum="1" Value="{Binding Level}"
                         Foreground="{StaticResource AccentBrush}" Background="#22FFFFFF" BorderThickness="0" />

            <Grid Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" Margin="0,0,4,0" ItemsSource="{Binding Modes}" SelectedItem="{Binding SelectedMode}" />
                <ComboBox Grid.Column="1" Margin="4,0,0,0" ItemsSource="{Binding Tones}" SelectedItem="{Binding SelectedTone}"
                          IsEnabled="{Binding IsToneEnabled}" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
